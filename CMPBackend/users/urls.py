from django.urls import path
# from . import views # Lo usaremos más adelante cuando crees tus vistas

urlpatterns = [
    # Aquí irán las rutas específicas de tu aplicación de usuarios
    # path('register/', views.register_user, name='register_user'),
    # path('profile/', views.user_profile, name='user_profile'),
]